# 可修改的 Java + C++ 源项目

本目录由 **native-obfuscator**(扩展分支)生成,包含混淆后 JAR 的**完整可修改源码**。

> **工具来源 / Fork 关系**
>
> | | |
> |---|---|
> | 上游原项目 | [radioegor146/native-obfuscator](https://github.com/radioegor146/native-obfuscator) |
> | 原作者 | radioegor146 and contributors |
> | 本 Fork | [xiaofanforfabric/native-obfuscator](https://github.com/xiaofanforfabric/native-obfuscator) |
> | 本 Fork 维护者 | xiaofanforfabric |
> | 许可证 | GNU GPL v3.0,含 Output Exception(与上游一致) |
>
> 上游原本只输出编译好的 `Loader.class`(黑盒,不可修改);
> 本 Fork **额外**输出本目录这套完整源码工程,便于自由修改。

## 目录结构

```
.
├── @JAR_NAME@                 # 混淆输出 JAR(执行入口)
├── build.sh                   # 一键重新编译 + 打包脚本
├── java/
│   └── @NATIVE_DIR@/
│       └── @LOADER_NAME@.java        # 加载器源码(可自由修改)
└── cpp/                       # 转译出的 C++ 原生代码
    ├── CMakeLists.txt
    ├── native_jvm.cpp/hpp
    ├── native_jvm_output.cpp
    ├── string_pool.cpp/hpp
    └── output/               # 每个 Java 类对应的 .cpp/.hpp
```

## 使用方式

1. 修改 `java/@NATIVE_DIR@/@LOADER_NAME@.java`,和/或修改 `cpp/` 下的任意 C++ 源码。
2. 运行一键脚本重新编译并打包:

```bash
chmod +x build.sh
./build.sh
```

脚本会依次:C++ 编译 → 计算原生库路径 → 编译 `@LOADER_NAME@.java` → 更新输出 JAR。

## 手动编译(可选)

```bash
# 编译 C++ 原生库
cmake -S cpp -B cpp/build -DCMAKE_BUILD_TYPE=Release
cmake --build cpp/build -j

# 编译 @LOADER_NAME@.java
javac -d java/build java/@NATIVE_DIR@/@LOADER_NAME@.java

# 更新 JAR(将原生库放入 @NATIVE_DIR@/<平台>-<类型>)
jar uf @JAR_NAME@ -C java/build @NATIVE_DIR@/@LOADER_NAME@.class
```

## 原生库命名规则

加载器会在运行时按 OS/架构查找库,条目名格式为 `@NATIVE_DIR@/<平台类型>-<系统类型>`,例如:

- Linux x64   → `@NATIVE_DIR@/x64-linux.so`
- Windows x64 → `@NATIVE_DIR@/x64-windows.dll`
- macOS arm64 → `@NATIVE_DIR@/arm64-macos.dylib`

## 许可证与归属

本目录内的 `Loader.java` 与 `cpp/` 代码源自 **GPL-3.0** 授权的
[radioegor146/native-obfuscator](https://github.com/radioegor146/native-obfuscator)
(上游原项目),并可能包含
[xiaofanforfabric/native-obfuscator](https://github.com/xiaofanforfabric/native-obfuscator)
(本 Fork)的修改。

**适用 Output Exception。** 上游 `LICENSE` 中对工具**输出**的 runtime 代码设有例外条款,
原文如下:

> Output Exception
>
> As a special exception, the copyright holders of native-obfuscator give
> unlimited permission to link, embed, compile and distribute the runtime code
> emitted by this tool -- including the native runtime (native_jvm), the loader,
> and the generated code fragments -- into your own programs, under terms of your
> choice, notwithstanding GPL-3.0 section 5(c).
>
> This exception applies only to the code emitted by the tool. The tool itself
> remains licensed under GPL-3.0 in full, without modification.

例外全文见上游 `LICENSE`:
<https://github.com/radioegor146/native-obfuscator/blob/master/LICENSE>

- 上游原始作品版权归原作者及贡献者所有
- 例外只覆盖工具**输出**的代码;工具本身仍是完整的 GPL-3.0
- 每条输出文件头部已带有该例外的声明注释,便于审计

> **范围说明**:例外原文列明的是 "the runtime code emitted by this tool",
> 即工具转译输出的 `java/` 与 `cpp/` 代码。本文件(`SOURCE_PROJECT.md`)与 `build.sh`
> 是本 Fork 为便于重新编译而额外输出的辅助文件,用途是配合上述代码使用。
