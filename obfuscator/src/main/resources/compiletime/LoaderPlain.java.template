/*
 * native-obfuscator 生成的加载器源文件 (LoaderPlain 模式)
 *
 * 该文件由 native-obfuscator 输出,供你自由修改。
 * 你可以修改此文件后,使用输出目录中的 build.sh 重新编译并打包。
 *
 * 注意: 本文件源自 GPL 3.0 授权的 radioegor146/native-obfuscator,
 *       修改与分发需遵循 GPL 3.0 许可证。
 */
package by.radioegor146.compiletime;

public class LoaderPlain {
    public static native void registerNativesForClass(int index, Class<?> clazz);

    static {
        System.loadLibrary("%LIB_NAME%");
    }
}
